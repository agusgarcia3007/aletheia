# Production Eval

Programmatic release gate for Aletheia as a verified agent. The suite is intentionally local and deterministic: no paid APIs, no live internet, and no external model dependency.

The current gate generates 100 checks across document QA, abstention, Go repair rules, repo QA, and research evidence policy. It is a production floor, not a claim of frontier-model parity.
