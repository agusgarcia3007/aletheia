# Mikros Live Eval

This suite checks the living Mikros layer: trainable routing, parametric local
answerers, strict research synthesis, abstention, and no raw chunk/link-only
responses in public chat.

Run:

```bash
go run ./cmd/aletheia eval --suite evals/mikros_live --json
```
