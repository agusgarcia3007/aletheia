# Bootstrap Eval Suite

Milestone 0 only validates that the suite path exists. Later milestones will add task files for compile fixes, test fixes, document QA, planning, abstention, and memory recall.

