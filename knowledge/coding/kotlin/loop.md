# Kotlin: como hacer un loop (for)

Para iterar con un loop for en Kotlin:

```kotlin
for (i in 1..5) {
    println(i)
}
for (item in lista) {
    println(item)
}
```

`1..5` es un rango inclusivo; tambien podes usar `until`, `step` y `downTo`.
